# Tracks Unfold

A mobile-first musical puzzle game about awakening an impossible instrument.

## Development

Use Node.js 24 and npm.

```sh
npm install
npm run dev
```

Run all validation with:

```sh
npm run check
```

Pushes to `main` are built and deployed to GitHub Pages.
